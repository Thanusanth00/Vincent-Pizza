# Vincent-Pizza
It's a Pizza store website
4. Open **phpMyAdmin**
5. Create a database (example: `vincent_pizza`)
6. Import the `.sql` file from the `database` folder
7. Update database credentials in `connect.php`
8. Open browser and go to:http://localhost/restaurant-website/
